apiVersion: apps/v1
kind: Deployment
metadata:
  name: hw-deploy
spec:
  replicas: 2  # Ajusta el número de replicas según tus necesidades
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 2
      maxUnavailable: 1
  selector:
    matchLabels:
      app: hw
  template:
    metadata:
      labels:
        app: hw
      annotations:
        notifications.argoproj.io/subscribe.on-sync-succeeded.slack: argocdnotification1
        notifications.argoproj.io/subscribe.on-sync-failed.slack: argocdnotification1
        notifications.argoproj.io/subscribe.on-health-degraded.slack: argocdnotification1
    spec:
      containers:
      - name: hw-container
        image: jaimehenao8126/kantox:206bad9051ac95192e7f6ece8402da4e1727b17c
        ports:
        - containerPort: 80
